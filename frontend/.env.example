# No frontend URL variables are required.
#
# Development uses Vite's /api and /storage proxies to Laravel on
# http://127.0.0.1:8000. Production uses the current origin, with the dashboard
# at /admin and Laravel's API at /api.
