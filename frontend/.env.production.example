# No variables are required for the production frontend build.
#
# Serve Laravel's public directory as the domain document root. Laravel mounts
# the dashboard at /admin, the API at /api, and uploaded files at /storage.
# Then run: npm run build
